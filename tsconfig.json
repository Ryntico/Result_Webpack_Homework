{
  "compilerOptions": {
    "outDir": "./dist/",
    // Указывает директорию, в которую будет сохраняться скомпилированный код.
    // Здесь это "./dist/".

    "noImplicitAny": true,
    // Запрещает использование неявного типа `any`.
    // Это требует явной типизации всех переменных и параметров.

    "module": "commonjs",
    // Устанавливает формат модулей CommonJS, который используется в Node.js.

    "target": "es2022",
    // Определяет версию JavaScript, в которую будет компилироваться код.
    // В данном случае это ES2022.

    "allowJs": true,
    // Позволяет компилятору TypeScript обрабатывать файлы JavaScript (`.js`)
    // в дополнение к TypeScript (`.ts`).

    "moduleResolution": "node",
    // Указывает, что для резолвинга модулей нужно использовать алгоритм Node.js.
    // Это стандарт для современных проектов.

    "esModuleInterop": true,
    // Включает совместимость с модулями ES6 для импорта CommonJS модулей.
    // Генерирует дополнительные вспомогательные функции.

    "forceConsistentCasingInFileNames": true,
    // Требует соблюдения единого регистра в названиях файлов при их импорте.
    // Это предотвращает ошибки на файловых системах с учетом регистра.

    "strict": true,
    // Включает строгий режим TypeScript, активирующий несколько проверок,
    // таких как `strictNullChecks` и `noImplicitAny`. Это рекомендуется для написания качественного кода.

    "skipLibCheck": true,
    // Пропускает проверку типов в файлах деклараций (`.d.ts`).
    // Это ускоряет компиляцию, но потенциально может скрыть ошибки.

    "typeRoots": [
      "./node_modules/@types",
      "./src/types"
    ]
    // Указывает каталоги, где компилятор будет искать типы.
    // В данном случае сначала проверяются стандартные типы в `node_modules/@types`,
    // а затем типы в директории `./src/types`.
  }
}

